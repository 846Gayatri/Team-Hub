[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["echo 'Skipping cloud build; using pre-built dist files'"]

[start]
cmd = "node backend/index.js"
