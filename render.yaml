services:
  - type: web
    name: team-hub
    env: node
    region: oregon
    plan: free
    buildCommand: "cd backend && npm install"
    startCommand: "node backend/index.js"
    envVars:
      - key: NODE_VERSION
        value: 22.14.0
      - key: JWT_SECRET
        generateValue: true
